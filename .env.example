# --- Base de datos -----------------------------------------------------------
POSTGRES_USER=um
POSTGRES_PASSWORD=um_local_dev
POSTGRES_DB=um_crm
POSTGRES_PORT=5433

DATABASE_URL="postgresql://um:um_local_dev@localhost:5433/um_crm?schema=public"

# --- Seguridad ---------------------------------------------------------------
# Genera uno nuevo con: openssl rand -base64 48
AUTH_SECRET="cambiar-este-secreto-por-uno-aleatorio-de-48-bytes-minimo"

# --- Archivos (escaneos de documentos) ---------------------------------------
STORAGE_DIR="./storage/archivos"

# --- Datos de la institución (salen en el PDF del tarjetón) ------------------
UM_NOMBRE="MUJERES UNIDAS"
UM_SEMANAS=12
